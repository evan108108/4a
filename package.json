{
  "name": "4a",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Reference implementation for 4A — Agent-Agnostic Accessible Archive. A convention on Nostr for AI-mediated public knowledge exchange.",
  "license": "Apache-2.0",
  "scripts": {
    "build:site": "node scripts/build-site.mjs",
    "dev": "npm run build:site && wrangler dev --config gateway/wrangler.toml",
    "deploy": "npm run build:site && wrangler deploy --config gateway/wrangler.toml",
    "tail": "wrangler tail --config gateway/wrangler.toml",
    "smoke-test": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260426.1",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "aws4fetch": "^1.0.20",
    "marked": "^18.0.2",
    "nostr-tools": "^2.23.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.85.0"
  },
  "engines": {
    "node": ">=20"
  }
}
